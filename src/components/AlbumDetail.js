//import libs
import React from 'react';
import { View } from 'react-native';
//make comp
const AlbumDetail = ({ children }) => {
  return (
    <View>
      {children}
    </View>
  );
};

//make it avail
export default AlbumDetail;
